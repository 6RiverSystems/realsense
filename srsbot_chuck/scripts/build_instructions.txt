# export LINUX_DISTRO=arm64
# export ROS_DISTRO=kinetic
#
# export LINUX_DISTRO=arm64
# export ROS_DISTRO=indigo
#
# export LINUX_DISTRO=armhf
# export ROS_DISTRO=indigo
#
# export LINUX_DISTRO=x86_64
# export ROS_DISTRO=indigo
#
# export LINUX_DISTRO=x86_64
# export ROS_DISTRO=kinetic
#
# Dockerhub Docker Image
# export ROS_BUILD_IMAGE=6river/rosbuild-$ROS_DISTRO-$LINUX_DISTRO
#
# OR
#
# Local Built Docker Image
# export ROS_BUILD_IMAGE=ros-$ROS_DISTRO-build-debian-$LINUX_DISTRO
#
# sudo docker run --rm -v /home/dan/mfp_workspace/$ROS_DISTRO-$LINUX_DISTRO:/mfp_workspace -v $PWD:/mfp_workspace/src -e CCACHE_DIR=/mfp_workspace/ccache --privileged -it $ROS_BUILD_IMAGE /bin/bash -C "/mfp_workspace/src/srsbot_chuck/scripts/build.sh"


# qemu build (armhf and aarch64)
#
# mkdir build
# cd build
# ../configure --target-list="arm-linux-user,aarch64-linux-user" --static --enable-fdt --enable-vhost-net --enable-kvm --extra-cflags="-DCONFIG_RTNETLINK"
# make -j40
#